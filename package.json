{
  "name": "yandex-speechkit-websocket-server",
  "version": "0.0.1",
  "main": "index.js",
  "author": "Pavel Fediukovich",
  "license": "MIT",
  "dependencies": {
    "@grpc/proto-loader": "^0.5.5",
    "google-proto-files": "^2.2.0",
    "grpc": "^1.24.3",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^13.11.1",
    "@types/socket.io": "^2.1.10",
    "gts": "^2.0.2",
    "ts-lint": "^4.5.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run check"
  }
}
